#include <stdio.h>

int main() {
    int a, b;

    // Taking input of two space-separated integers
    scanf("%d %d", &a, &b);

    // Calculating and printing the sum
    printf("Sum: %d\n", a + b);

    return 0;
}
