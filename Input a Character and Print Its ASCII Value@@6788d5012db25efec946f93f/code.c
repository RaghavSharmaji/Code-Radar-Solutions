#include <stdio.h>

int main() {
    char ch;

    // Taking input of a single character
    scanf("%c", &ch);

    // Printing the ASCII value of the character
    printf("ASCII Value: %d\n", ch);

    return 0;
}
